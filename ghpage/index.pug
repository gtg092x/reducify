extends ./layout.pug

block title
    title Reducify | Make reducers with less effort

block nav
    a.nav-link(href='#installation') Install
    a.nav-link(href='#usage') Usage
    a.nav-link(href='#reducers-made-three-ways') Reducers
    a.nav-link(href='#configuration-sugar') Configuration
    a.nav-link(href='#credits') Credits

block github
    a.nav-link.github(href='https://github.com/gtg092x/reducify',target="_blank")
        span.fa.fa-github
        | Github

block analytics
    script.
        (function (i, s, o, g, r, a, m) {
            i['GoogleAnalyticsObject'] = r;
            i[r] = i[r] || function () {
                        (i[r].q = i[r].q || []).push(arguments)
                    }, i[r].l = 1 * new Date();
            a = s.createElement(o),
                    m = s.getElementsByTagName(o)[0];
            a.async = 1;
            a.src = g;
            m.parentNode.insertBefore(a, m)
        })(window, document, 'script', 'https://www.google-analytics.com/analytics.js', 'ga');

        ga('create', 'UA-44186754-3', 'auto');
        ga('send', 'pageview');

block example
    // empty

block masthead
    h1.container
        span.mast-card
            span.text Less effort, more reducers
            pre
                code.
                    reducify({
                       "ADD": (state, action) => state + action.data,
                       "SUBTRACT": (state, action) => state - action.data
                    })
block scripts
    script.
        $('a[href="http://reducify.mediadrake.com"]').remove();
